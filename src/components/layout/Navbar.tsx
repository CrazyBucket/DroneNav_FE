import React from "react";

const Navbar = () => (
  <header className="h-[36px] backdrop-blur-md bg-white/10 border border-gray-600/50 text-white p-4 mx-2 mt-2 flex items-center rounded-xl hover:bg-white/20 transition-all duration-300 shadow-lg shadow-black/10">
    <span className="text-xl font-bold text-white/90 tracking-wider">
      DroneNav
    </span>
  </header>
);

export default Navbar;
